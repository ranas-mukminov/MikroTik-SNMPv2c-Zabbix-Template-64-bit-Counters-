# MikroTik SNMP шаблоны для Zabbix (64-битные счётчики)

Готовые к использованию шаблоны Zabbix для мониторинга устройств MikroTik RouterOS с 64-битными счётчиками интерфейсов, поддержка SNMPv2c и SNMPv3.

[English](README.md) | **Русский**

---

## 👤 Об авторе

**Ранас Мукминов**  
DevOps / SRE / Сетевой инженер

- 🌐 Сайт: [run-as-daemon.ru](https://run-as-daemon.ru)
- 💼 GitHub: [@ranas-mukminov](https://github.com/ranas-mukminov)
- 💬 Telegram: [Контакты на сайте](https://run-as-daemon.ru)

---

## 📖 Что это за репозиторий?

Данный репозиторий предоставляет **профессиональные шаблоны Zabbix** для мониторинга устройств MikroTik RouterOS через SNMP. Шаблоны ориентированы на точный мониторинг сетевых интерфейсов с использованием **64-битных счётчиков** (ifHC*) для предотвращения переполнения счётчиков на высокоскоростных интерфейсах.

Основные возможности:
- **64-битные счётчики интерфейсов** (ifHCInOctets, ifHCOutOctets) для точного измерения трафика на каналах 1G+
- **Варианты шаблонов SNMPv2c и SNMPv3** для различных требований безопасности
- **Всесторонний мониторинг**: состояние системы (CPU, память, температура), метрики интерфейсов (трафик, ошибки, отбросы, статус), аппаратная инвентаризация
- **Совместимость с Zabbix 7.x и 6.0 LTS** с минимальными правками
- **Поддержка RouterOS 6.x и 7.x** с использованием специфичных для MikroTik OID

---

## 📁 Структура репозитория

```
MikroTik-SNMPv2c-Zabbix-Template-64-bit-Counters-/
├── template_mikrotik_snmpv2c_advanced_zbx72.xml    # Основной шаблон SNMPv2c (расширенный)
├── template_mikrotik_snmpv2c_zbx72_uuid32.xml      # Шаблон SNMPv2c (вариант UUID32)
├── template_mikrotik_snmpv3_advanced_zbx72.xml     # Шаблон SNMPv3 (рекомендуется)
├── examples/
│   ├── mikrotik_snmp_config.rsc                    # Скрипт конфигурации SNMP для RouterOS
│   ├── zabbix_host_config_example.md               # Руководство по настройке узлов Zabbix
│   └── README.md                                    # Документация примеров
├── tests/                                           # Тесты валидации шаблонов
├── node-exporter-full-stack/                       # Опциональная интеграция с Prometheus
├── CHANGELOG.md                                     # История версий
├── README.md                                        # Английская документация
└── README.ru.md                                     # Этот файл
```

### Доступные шаблоны

| Файл | Протокол | Версия Zabbix | Описание |
|------|----------|---------------|----------|
| `template_mikrotik_snmpv2c_advanced_zbx72.xml` | SNMPv2c | 7.2+ | Полнофункциональный шаблон с мониторингом системы, интерфейсов, аппаратных датчиков |
| `template_mikrotik_snmpv2c_zbx72_uuid32.xml` | SNMPv2c | 7.2+ | Упрощённый шаблон с совместимостью UUID32 |
| `template_mikrotik_snmpv3_advanced_zbx72.xml` | SNMPv3 | 7.2+ | Защищённый шаблон с аутентификацией и шифрованием (рекомендуется для продакшена) |

---

## ⚙️ Требования

### Сервер Zabbix
- **Zabbix 7.2+** (рекомендуется)
- **Zabbix 6.0 LTS** (совместим с минимальными правками шаблона)

### MikroTik RouterOS
- **RouterOS 7.x** (рекомендуется)
- **RouterOS 6.x** (поддерживается, но 7.x рекомендуется для лучшей стабильности SNMP)

### Настройка SNMP
- **SNMPv2c**: Простая аутентификация на основе community (подходит для изолированных сетей)
- **SNMPv3**: Аутентификация (SHA/MD5) + шифрование (AES/DES) - **рекомендуется для продакшена**
- Настройка SNMP выполняется на устройстве MikroTik (на стороне хоста), шаблоны остаются неизменными

### Сетевые требования
- UDP порт **161** должен быть доступен с сервера/прокси Zabbix до устройства MikroTik
- Правила файрвола на MikroTik должны разрешать SNMP только с доверенного IP-адреса сервера Zabbix

---

## 🚀 Быстрый старт

### Шаг 1: Включение SNMP на MikroTik

#### Конфигурация SNMPv2c (Базовая)

Подключитесь к вашему MikroTik через терминал и выполните:

```routeros
# Включить службу SNMP
/snmp set enabled=yes contact="admin@example.com" location="ЦОД-1"

# Настроить SNMP community с ограничением по IP
# БЕЗОПАСНОСТЬ: Измените "MySecureString123!" на надёжную случайную строку
# БЕЗОПАСНОСТЬ: Измените "10.0.0.100" на IP-адрес вашего сервера Zabbix
/snmp community
set [find default=yes] name="MySecureString123!" addresses=10.0.0.100/32 read-access=yes write-access=no

# Добавить правило файрвола для ограничения доступа SNMP
/ip firewall filter
add chain=input protocol=udp dst-port=161 src-address=10.0.0.100/32 action=accept comment="Разрешить SNMP от Zabbix"
add chain=input protocol=udp dst-port=161 action=drop comment="Блокировать остальной SNMP"
```

**Полные скрипты конфигурации** см. в [`examples/mikrotik_snmp_config.rsc`](examples/mikrotik_snmp_config.rsc).

### Шаг 2: Импорт шаблона в Zabbix

1. Откройте веб-интерфейс Zabbix
2. Перейдите в **Настройка** → **Шаблоны**
3. Нажмите кнопку **Импорт**
4. Выберите файл XML шаблона (например, `template_mikrotik_snmpv2c_advanced_zbx72.xml`)
5. Проверьте параметры импорта и нажмите **Импорт**
6. Убедитесь, что шаблон появился в списке шаблонов

### Шаг 3: Привязка шаблона к узлу MikroTik

1. Перейдите в **Настройка** → **Узлы сети**
2. Создайте новый узел или отредактируйте существующий узел MikroTik
3. Настройте параметры узла:
   - **Имя узла**: `mikrotik-router-01` (или имя вашего устройства)
   - **Группы**: Создайте/выберите группу `MikroTik Routers`
   - **Интерфейсы**: Добавьте интерфейс **SNMP**
     - **IP-адрес**: IP-адрес управления вашего MikroTik
     - **Порт**: 161
     - **Версия SNMP**: SNMPv2
     - **SNMP community**: `{$SNMP_COMMUNITY}` (или введите вашу community-строку)
4. Перейдите на вкладку **Шаблоны**
5. Нажмите **Выбрать** и выберите импортированный шаблон
6. Нажмите **Добавить**, затем **Обновить**

### Шаг 4: Проверка сбора данных

1. Подождите 2-3 минуты для начального сбора данных
2. Перейдите в **Мониторинг** → **Последние данные**
3. Отфильтруйте по вашему узлу MikroTik
4. Проверьте наличие трафика интерфейсов, uptime системы, CPU и метрик памяти
5. Перейдите в **Мониторинг** → **Проблемы** для проверки работы триггеров

**Подробное руководство по настройке**: [`examples/zabbix_host_config_example.md`](examples/zabbix_host_config_example.md)

---

## 📊 Что мониторится

### Состояние системы
- **Uptime**: Отслеживание времени работы устройства с обнаружением перезагрузок
- **CPU**: Процент загрузки процессора (предупреждение на 70%, критично на 90%)
- **Память**: Общая, используемая, свободная память с процентом использования
- **Диск**: Использование хранилища (где применимо)
- **Температура**: Датчик температуры системы (специфичный для MikroTik OID)
- **Напряжение**: Мониторинг напряжения системы (специфичный для MikroTik OID)

### Сетевые интерфейсы (64-битные счётчики)
- **Трафик**: Входящие/исходящие октеты (байты) с использованием ifHCInOctets/ifHCOutOctets
- **Утилизация канала**: Расчётный процент от пропускной способности интерфейса
- **Скорость интерфейса**: Настроенная скорость (ifHighSpeed) для планирования ёмкости
- **Операционный статус**: up/down/testing/unknown/dormant/notPresent/lowerLayerDown
- **Административный статус**: Настроенное административное состояние
- **Ошибки**: Входящие/исходящие ошибки на интерфейс
- **Отбросы**: Отброшенные пакеты из-за переполнения буфера
- **Broadcast/Multicast**: Обнаружение штормов с настраиваемыми порогами

### Аппаратная инвентаризация
- **Версия RouterOS**: Версия операционной системы (автоматическое обнаружение)
- **Серийный номер устройства**: Серийный номер оборудования (специфичный для MikroTik OID)
- **Имя системы**: Настроенное имя хоста (sysName)
- **Местоположение системы**: Физическое местоположение (sysLocation)
- **Контакт системы**: Контактная информация (sysContact)

### Автоматическое обнаружение (LLD)
- **Обнаружение интерфейсов**: Автоматическое обнаружение всех сетевых интерфейсов
- **Фильтрация интерфейсов**: Исключение loopback, отключённых или конкретных интерфейсов через макросы regex

---

## 🔐 Пример конфигурации SNMPv3

SNMPv3 обеспечивает аутентификацию и шифрование, что делает его **рекомендуемым для производственных сред**.

### Конфигурация MikroTik

```routeros
# Включить службу SNMP
/snmp set enabled=yes contact="admin@example.com" location="ЦОД-1" trap-version=3

# Создать пользователя SNMPv3 с аутентификацией и шифрованием
# БЕЗОПАСНОСТЬ: Измените пароли на надёжные случайные строки (минимум 8 символов)
# БЕЗОПАСНОСТЬ: Измените "10.0.0.100" на IP-адрес вашего сервера Zabbix
/snmp community
add name=zabbix_monitor \
    authentication-protocol=SHA1 \
    encryption-protocol=AES \
    authentication-password="Ваш_Пароль_Аутентификации_Мин8Симв!" \
    encryption-password="Ваш_Пароль_Шифрования_Мин8Симв!" \
    addresses=10.0.0.100/32 \
    read-access=yes \
    write-access=no

# Добавить правило файрвола для ограничения доступа SNMP
/ip firewall filter
add chain=input protocol=udp dst-port=161 src-address=10.0.0.100/32 action=accept comment="Разрешить SNMP от Zabbix"
add chain=input protocol=udp dst-port=161 action=drop comment="Блокировать остальной SNMP"
```

### Конфигурация узла в Zabbix

При создании узла в Zabbix:

1. **Настройки интерфейса SNMP**:
   - **Версия SNMP**: SNMPv3
   - **Имя контекста**: (оставить пустым)
   - **Имя безопасности**: `zabbix_monitor`
   - **Уровень безопасности**: authPriv

2. **Настройки аутентификации**:
   - **Протокол аутентификации**: SHA
   - **Парольная фраза аутентификации**: `Ваш_Пароль_Аутентификации_Мин8Симв!`

3. **Настройки конфиденциальности**:
   - **Протокол конфиденциальности**: AES
   - **Парольная фраза конфиденциальности**: `Ваш_Пароль_Шифрования_Мин8Симв!`

### Тестирование SNMPv3 с сервера Zabbix

```bash
# Тест соединения SNMPv3
snmpwalk -v3 -l authPriv \
  -u zabbix_monitor \
  -a SHA -A "Ваш_Пароль_Аутентификации_Мин8Симв!" \
  -x AES -X "Ваш_Пароль_Шифрования_Мин8Симв!" \
  <mikrotik-ip> system

# Тест конкретного OID (uptime системы)
snmpget -v3 -l authPriv \
  -u zabbix_monitor \
  -a SHA -A "Ваш_Пароль_Аутентификации_Мин8Симв!" \
  -x AES -X "Ваш_Пароль_Шифрования_Мин8Симв!" \
  <mikrotik-ip> SNMPv2-MIB::sysUpTime.0
```

---

## 🔧 Настройка отображений в Zabbix (value mappings)

Правильные сопоставления значений и предобработка делают метрики понятными и практичными.

### Сопоставление операционного статуса интерфейса

Создайте сопоставление значений для статуса интерфейса:

**Имя карты значений**: `IfOperStatus`

| Значение | Отображается как |
|----------|------------------|
| 1 | up (работает) |
| 2 | down (не работает) |
| 3 | testing (тестирование) |
| 4 | unknown (неизвестно) |
| 5 | dormant (спящий) |
| 6 | notPresent (отсутствует) |
| 7 | lowerLayerDown (нижний уровень недоступен) |

**Как применить**:
1. Перейдите в **Настройка** → **Шаблоны** → Ваш шаблон
2. Найдите элемент данных `ifOperStatus[{#IFNAME}]`
3. В настройках элемента установите **Показать значение**: `IfOperStatus`
4. Это также применяется к `ifAdminStatus[{#IFNAME}]`

### Человекочитаемая скорость интерфейса

Преобразование скорости интерфейса из bps в Mbps:

**Элемент данных**: `ifHighSpeed[{#IFNAME}]`
- **Предобработка**: Пользовательский множитель → `0.000001` (преобразует bps в Mbps)
- **Единицы**: `Mbps`
- **Тип значения**: Числовой (с плавающей точкой)

Результат: Скорость интерфейса отображается как "1000 Mbps" вместо "1000000000"

### Сопоставление типов интерфейсов MikroTik

Создайте сопоставление значений для специфичных типов интерфейсов MikroTik:

**Имя карты значений**: `MikroTik.Interface.Type`

| Значение | Отображается как |
|----------|------------------|
| 6 | ethernetCsmacd (Ethernet) |
| 24 | softwareLoopback (программный loopback) |
| 53 | propVirtual (виртуальный) |
| 131 | tunnel (туннель) |
| 135 | l2vlan (VLAN L2) |
| 136 | l3ipvlan (VLAN L3) |
| 244 | pppoe (PPPoE) |

**Применить к элементу**: `ifType[{#IFNAME}]`

### Триггеры утилизации канала

Используйте макросы для определения порогов утилизации интерфейсов:

**Рекомендуемые макросы**:
- `{$IF_UTIL_WARN}` = `70` (Предупреждение при 70% утилизации)
- `{$IF_UTIL_HIGH}` = `90` (Критично при 90% утилизации)
- `{$IF_ERROR_RATE_WARN}` = `1` (Предупреждение при ошибках/отбросах в секунду)

**Пример выражения триггера** (предупреждение о входящей утилизации):
```
avg(/Template/ifHCInOctets[{#IFNAME}],5m) / ifHighSpeed[{#IFNAME}] * 8 * 100 > {$IF_UTIL_WARN}
```

**Пример выражения триггера** (предупреждение о скорости ошибок):
```
rate(/Template/ifInErrors[{#IFNAME}],5m) > {$IF_ERROR_RATE_WARN}
```

### Пример конвейера предобработки

**Элемент данных**: Входящий трафик интерфейса (bps)

1. **Изменение в секунду**: Преобразование накопительного счётчика в скорость
2. **Пользовательский множитель**: `8` (преобразование байтов в биты)
3. **Единицы**: `bps`

**Элемент данных**: Расчёты процентов

1. **Формула вычисляемого элемента**: `(last(//ifHCInOctets[{#IFNAME}]) / last(//ifHighSpeed[{#IFNAME}])) * 100`
2. **Единицы**: `%`

### Макросы узла для настройки

Определите макросы на уровне узла для настройки порогов на каждое устройство:

| Макрос | По умолчанию | Описание |
|--------|--------------|----------|
| `{$SNMP_COMMUNITY}` | `CHANGE_ME_SNMPV2C` | Строка community SNMPv2c |
| `{$CPU_UTIL_WARN}` | `70` | Порог предупреждения загрузки CPU (%) |
| `{$CPU_UTIL_CRIT}` | `90` | Критический порог загрузки CPU (%) |
| `{$MEM_UTIL_WARN}` | `80` | Порог предупреждения использования памяти (%) |
| `{$MEM_UTIL_CRIT}` | `95` | Критический порог использования памяти (%) |
| `{$TEMP_WARN}` | `60` | Порог предупреждения температуры (°C) |
| `{$TEMP_CRIT}` | `75` | Критический порог температуры (°C) |
| `{$IF_UTIL_WARN}` | `70` | Предупреждение утилизации интерфейса (%) |
| `{$IF_UTIL_HIGH}` | `90` | Критическая утилизация интерфейса (%) |
| `{$IF_ERROR_RATE_WARN}` | `1` | Предупреждение скорости ошибок интерфейса (ошибок/сек) |
| `{$IFCONTROL}` | `1` | Фильтр обнаружения интерфейсов (1=включено) |

**Переопределение на уровне узла**: Настройка → Узлы сети → Ваш узел → вкладка Макросы

---

## 🛠️ Решение типовых проблем

### Нет данных от узла

**Симптомы**: Элементы данных показывают "Нет данных" или "Не поддерживается"

**Решения**:
1. **Проверьте связь SNMP** с сервера Zabbix:
   ```bash
   snmpwalk -v2c -c ВашCommunity <mikrotik-ip> system
   ```
2. **Проверьте правила файрвола** на MikroTik:
   ```routeros
   /ip firewall filter print where dst-port=161
   ```
3. **Убедитесь, что SNMP включён** на MikroTik:
   ```routeros
   /snmp print
   ```
4. **Проверьте соответствие SNMP community/учётных данных** между MikroTik и настройками узла в Zabbix

### Интерфейсы не обнаружены

**Симптомы**: Автоматическое обнаружение не находит интерфейсы или отсутствуют ожидаемые интерфейсы

**Решения**:
1. **Проверьте правила LLD**: Настройка → Шаблоны → Правила обнаружения → Обнаружение интерфейсов
2. **Просмотрите фильтры интерфейсов**: Проверьте макросы типа `{$IFCONTROL}`, `{$NET.IF.IFNAME.MATCHES}`, `{$NET.IF.IFNAME.NOT_MATCHES}`
3. **Проверьте версию SNMP**: Убедитесь, что версия SNMP узла соответствует ожиданиям шаблона
4. **Протестируйте OID интерфейса вручную**:
   ```bash
   snmpwalk -v2c -c ВашCommunity <mikrotik-ip> ifDescr
   ```
5. **Проверьте версию RouterOS**: Некоторые старые версии имеют ограниченную поддержку SNMP

### Скачки трафика или странные значения

**Симптомы**: Нереалистичные графики трафика, внезапные скачки или отрицательные значения

**Решения**:
1. **Убедитесь, что используются 64-битные счётчики**: Шаблоны используют `ifHCInOctets`/`ifHCOutOctets` (64-бит) вместо `ifInOctets`/`ifOutOctets` (32-бит)
2. **Проверьте переполнение счётчиков**: 32-битные счётчики переполняются на ~4ГБ на высокоскоростных каналах
3. **Убедитесь в отсутствии перекрывающихся шаблонов**: Несколько шаблонов, собирающих одинаковые метрики, могут вызывать конфликты
4. **Проверьте предобработку**: Убедитесь, что "Изменение в секунду" применяется к накопительным счётчикам
5. **Проверьте SNMP-агент RouterOS**: Обновите до последней стабильной версии RouterOS, если проблемы сохраняются

### Таймауты SNMP

**Симптомы**: Частые ошибки "SNMP timeout" в Zabbix

**Решения**:
1. **Увеличьте таймаут SNMP**: Узел → Интерфейсы → SNMP → Таймаут (по умолчанию 3с, попробуйте 5-10с)
2. **Проверьте задержку сети**: Высокая задержка или потеря пакетов влияет на время отклика SNMP
3. **Уменьшите частоту опроса**: Настройте интервалы элементов данных, если устройство под высокой нагрузкой
4. **Загрузка CPU на MikroTik**: Высокое использование CPU на MikroTik может задерживать ответы SNMP

### Высокая загрузка CPU на MikroTik

**Симптомы**: SNMP-запросы вызывают скачки CPU на MikroTik

**Решения**:
1. **Увеличьте интервалы обновления элементов**: Измените с 1м на 2м или 5м для менее критичных метрик
2. **Отключите ненужные правила обнаружения**: Закомментируйте ненужные правила LLD
3. **Используйте SNMPv2c вместо v3** (если позволяет безопасность): шифрование v3 добавляет нагрузку на CPU
4. **Обновите RouterOS**: Новые версии имеют улучшенную производительность SNMP

---

## 🗺️ Дорожная карта

### Планируемые функции
- [ ] Подробная документация метрик для каждого элемента шаблона
- [ ] Примеры дашбордов Grafana для визуализации MikroTik
- [ ] Расширенные примеры SNMPv3 (различные комбинации auth/priv)
- [ ] Тестирование совместимости с RouterOS 8.x (когда выйдет)
- [ ] Коллекция SNMP-шаблонов для разных вендоров
- [ ] Оптимизация шаблонов для Zabbix 7.4+ LTS
- [ ] Интеграция с API MikroTik как альтернатива SNMP
- [ ] Автоматизированный CI/CD конвейер валидации шаблонов

### Рассматривается
- Примеры конфигурации приёмника SNMP trap
- Шаблоны мониторинга беспроводных интерфейсов
- Мониторинг протоколов маршрутизации BGP/OSPF
- Шаблоны мониторинга очередей и QoS
- Мониторинг PPPoE-сервера
- Шаблоны контроллера беспроводных точек CAPsMAN

**Предложения приветствуются!** Откройте issue для обсуждения новых функций или улучшений.

---

## 🤝 Как помочь проекту

Вклады приветствуются и ценятся! Вот как вы можете помочь:

### Как внести вклад

1. **Сделайте fork репозитория**
2. **Создайте ветку функции**: `git checkout -b feature/ваше-имя-функции`
3. **Внесите изменения**:
   - Обновите шаблоны (XML-файлы)
   - Добавьте/улучшите документацию
   - Исправьте баги или проблемы
4. **Протестируйте изменения**:
   - Импортируйте шаблоны в тестовый экземпляр Zabbix
   - Проверьте работу мониторинга на тестовом устройстве MikroTik
   - Задокументируйте протестированные версии (Zabbix X.Y, RouterOS Z.W)
5. **Commit с понятными сообщениями**: `git commit -m "Добавлено: Пример SNMPv3 для RouterOS 7.x"`
6. **Push в ваш fork**: `git push origin feature/ваше-имя-функции`
7. **Откройте Pull Request**: Опишите ваши изменения и тестовое окружение

### Руководство по вкладу

- **Опишите тестовое окружение** в PR: версия Zabbix, версия RouterOS, модель устройства
- **Следуйте существующей структуре шаблона**: Сохраняйте соглашения об именовании
- **Обновляйте документацию**: При добавлении функций обновляйте README.md и README.ru.md
- **Безопасность прежде всего**: Никогда не коммитьте реальные учётные данные или IP-адреса
- **Простота**: Предпочитайте ясность изощрённости

### Что можно внести

- Новые варианты шаблонов (разные версии Zabbix, специализированные случаи использования)
- Исправления багов в элементах шаблонов или триггерах
- Улучшения документации (опечатки, ясность, переводы)
- Примеры конфигураций и случаев использования
- Советы по устранению неполадок из реального опыта

---

## 📄 Лицензия

Этот проект предоставляется для **личного и коммерческого использования** без ограничений.

**Условия**:
- Используйте на свой риск; гарантии не предоставляются
- Атрибуция приветствуется, но не требуется
- Можете свободно модифицировать и распространять
- Не несём ответственности за любые проблемы, возникающие при использовании шаблонов

**Лучшие практики**:
- Всегда тестируйте шаблоны в непродакшен-окружении сначала
- Создавайте резервные копии конфигурации Zabbix перед импортом новых шаблонов
- Проверяйте элементы шаблонов и триггеры под ваши конкретные нужды
- Держите шаблоны обновлёнными с последними версиями Zabbix и RouterOS

---

## 💬 Поддержка и контакты

- **Issues**: [GitHub Issues](https://github.com/ranas-mukminov/MikroTik-SNMPv2c-Zabbix-Template-64-bit-Counters-/issues)
- **Обсуждения**: [GitHub Discussions](https://github.com/ranas-mukminov/MikroTik-SNMPv2c-Zabbix-Template-64-bit-Counters-/discussions)
- **Профессиональные услуги**: [run-as-daemon.ru](https://run-as-daemon.ru)

Для профессионального консалтинга по развёртыванию Zabbix, проектированию сетей MikroTik или корпоративных решений мониторинга посетите [run-as-daemon.ru](https://run-as-daemon.ru).

---

**Сделано с ❤️ [Ранасом Мукминовым](https://github.com/ranas-mukminov)**
